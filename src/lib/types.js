/**
 * @typedef {Object} User
 * @property {string} userId - Kullanıcı ID'si
 * @property {string} username - Kullanıcı adı
 * @property {string} role - Kullanıcı rolü ('user' | 'admin' | 'founder')
 * @property {string} [avatar] - Kullanıcı avatar URL'si (opsiyonel)
 */

/**
 * @typedef {'user' | 'admin' | 'founder'} UserRole
 */

export {}; 